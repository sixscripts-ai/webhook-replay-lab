# Default for local dev: SQLite. Switch to Postgres by changing the provider in prisma/schema.prisma
# and using a postgres connection string here.
DATABASE_URL="file:./prisma/dev.db"

# Base URL used by the app to construct full URLs in server components.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
